//creating a card skeleton component and then passing props to it
import React from "react";

const CompositionReact = (props) => {
  return <div>{props.children}</div>;
};

export default CompositionReact;
